import fs from 'node:fs';
import path from 'node:path';
import cp from 'node:child_process';
import assert from 'node:assert/strict';
import { getWebSocketConstructor } from './websocket-client.mjs';

const root=path.resolve(path.dirname(new URL(import.meta.url).pathname),'..');
const valueSource=fs.readFileSync(path.join(root,'src/core/valueController.js'),'utf8');
const dateSource=fs.readFileSync(path.join(root,'src/components/date-picker.js'),'utf8');
const css=fs.readFileSync(path.join(root,'src/qxframe9a7c2.css'),'utf8');
assert.doesNotMatch(valueSource,/controlled:\s*Object\.prototype\.hasOwnProperty\.call\([^\n]*['"]value['"]/, 'ValueController option binding must not infer controlled ownership from value.');
assert.match(dateSource,/function rememberCalendarDrillOwner\(detail\)/,'DatePicker dual-panel drill must preserve the physical panel owner independently from range edit ownership.');
assert.match(dateSource,/function restoreDateKeyboardAnchor\(fallback, source\)/,'DatePicker must restore one canonical keyboard anchor after year\/month drill.');
assert.match(dateSource,/draft\.draftValue\[0\]/,'DatePicker range keyboard restoration must consider the range start.');
assert.match(dateSource,/sameCalendarMonth\(start, primaryView\)/,'DatePicker must reuse range start when it remains visible in the primary panel.');
assert.match(dateSource,/sameCalendarMonth\(start, secondaryView\)/,'DatePicker must reuse range start when it remains visible in the secondary panel.');
assert.match(dateSource,/date = cloneDate\(fallback\)/,'DatePicker must continue from the drilled month when range start is outside the visible pair.');
assert.match(dateSource,/restoreDateKeyboardAnchor\(selected, source\)/,'DatePicker month drill must restore the canonical date keyboard anchor.');
assert.match(css,/\.qxframe9a7c2-date-picker-presets\.is-virtual-focus-owner:focus-visible\s*\{[^}]*outline\s*:\s*none/, 'DatePicker preset real-focus owner outline must be suppressed.');
assert.match(css,/\.qxframe9a7c2-date-picker-preset\.is-keyboard-focus:not\(:disabled\)\s*\{[^}]*outline\s*:/, 'DatePicker preset virtual focus item must own the visible outline.');

const browser=[process.env.CHROMIUM_BIN,'/usr/bin/chromium','/usr/bin/chromium-browser','/usr/bin/google-chrome'].filter(Boolean).find(fs.existsSync);
if(!browser){console.log(JSON.stringify({ok:true,structural:true,browserSkipped:true,reason:'chromium not found'}));process.exit(0);}
const WebSocketClient=await getWebSocketConstructor();
const depRe=/(?:\bimport\s*(?:[^'";]*?\s+from\s*)?|\bexport\s+[^'";]*?\s+from\s*)['"]([^'"]+)['"]/g;
const dynamicRe=/\bimport\s*\(\s*['"]([^'"]+)['"]\s*\)/g;
const files=new Map();
function normalize(file){return path.relative(root,file).split(path.sep).join('/');}
function resolve(from,spec){if(!spec.startsWith('.'))return null;let file=path.resolve(path.dirname(from),spec);if(!path.extname(file))file+='.js';return file;}
function collect(file){file=path.resolve(file);if(files.has(file))return;let src=fs.readFileSync(file,'utf8');const specs=[];for(const re of [depRe,dynamicRe]){re.lastIndex=0;let m;while((m=re.exec(src)))if(m[1].startsWith('.'))specs.push(m[1]);}files.set(file,src);for(const spec of specs)collect(resolve(file,spec));}
collect(path.join(root,'src/index.js'));
const imports={};
for(const [file,raw] of files){let src=raw;for(const re of [depRe,dynamicRe])src=src.replace(re,(full,spec)=>{const resolved=resolve(file,spec);return resolved?full.replace(spec,'qx:/'+normalize(resolved)):full;});src+='\n//# sourceURL=qx:/'+normalize(file);imports['qx:/'+normalize(file)]='data:text/javascript;base64,'+Buffer.from(src).toString('base64');}
const importMap=JSON.stringify({imports}).replace(/</g,'\\u003c');
const safeCss=css.replace(/<\/style/gi,'<\\/style');
const test=`import {initializeRuntime,InputOTP,TagInput,Select,Ripple,DatePicker} from 'qx:/src/index.js';
const out=document.getElementById('result');const sleep=ms=>new Promise(r=>setTimeout(r,ms));function a(v,m){if(!v)throw new Error(m)}function host(){const h=document.createElement('div');document.body.appendChild(h);return h;}function key(el,k){el.dispatchEvent(new KeyboardEvent('keydown',{key:k,bubbles:true,cancelable:true}));}
try{initializeRuntime(globalThis);
const otp=InputOTP.create({document,container:host(),length:4,value:'1234'});a(otp.getState().controlled===false,'InputOTP value alone must remain mutable');const oi=otp.getInputElements()[0];oi.focus();oi.value='9';oi.dispatchEvent(new Event('input',{bubbles:true}));await sleep(0);a(otp.getState().value==='9234','InputOTP user input must commit when initialized with value');otp.destroy();
const ti=TagInput.create({document,container:host(),value:[{key:'a',value:'A',label:'A'}]});a(ti.getState().controlled===false,'TagInput value alone must remain mutable');ti.add('B',{source:'keyboard',reason:'verify'});await sleep(0);a(ti.getState().value.map(t=>t.value).join(',')==='A,B','TagInput add must commit when initialized with value');ti.destroy();
const sel=Select.create({document,container:host(),items:[{value:'a',label:'A'},{value:'b',label:'B'}],value:'a'});a(sel.getState().controlled===false,'Select value alone must remain mutable');sel.getOptionList().setValue('b',{source:'keyboard',reason:'verify'});await sleep(0);a(sel.getState().value==='b','Select option change must commit when initialized with value');sel.destroy();
const rh=document.createElement('button');rh.type='button';rh.style.cssText='width:100px;height:40px;--qxframe9a7c2-ripple-enter-duration:80ms;--qxframe9a7c2-ripple-leave-duration:80ms;--qxframe9a7c2-ripple-outside-duration:120ms';document.body.appendChild(rh);const ripple=Ripple.enhance(rh,{inside:true,outside:true});await sleep(50);if(ripple.getState().motionEnabled){rh.dispatchEvent(new PointerEvent('pointerdown',{bubbles:true,clientX:10,clientY:10,pointerId:1,pointerType:'mouse'}));a(rh.querySelectorAll('.is-inside').length===1&&rh.querySelectorAll('.is-outside').length===0,'Ripple press must create inside only');await sleep(110);a(rh.querySelectorAll('.is-inside').length===1&&rh.querySelector('.is-inside')&&!rh.querySelector('.is-inside').classList.contains('is-leave'),'Inside ripple must finish expansion and remain until release; html='+rh.innerHTML+' state='+JSON.stringify(ripple.getState()));rh.dispatchEvent(new PointerEvent('pointerup',{bubbles:true,clientX:10,clientY:10,pointerId:1,pointerType:'mouse'}));a(rh.querySelectorAll('.is-outside').length===1&&rh.querySelector('.is-inside').classList.contains('is-leave'),'Ripple release must fade inside and start outside');await sleep(20);rh.dispatchEvent(new PointerEvent('pointerdown',{bubbles:true,clientX:20,clientY:10,pointerId:2,pointerType:'mouse'}));rh.dispatchEvent(new PointerEvent('pointerup',{bubbles:true,clientX:20,clientY:10,pointerId:2,pointerType:'mouse'}));a(rh.querySelectorAll('.qxframe9a7c2-ripple-wave').length>=3,'Rapid clicks must preserve overlapping ripple waves');await sleep(260);a(rh.querySelectorAll('.qxframe9a7c2-ripple-wave').length===0,'Ripple waves must clean up after their own animations');}ripple.destroy();
const autoButton=document.createElement('button');autoButton.type='button';autoButton.className='qxframe9a7c2-button is-ripple';autoButton.style.cssText='width:100px;height:40px;--qxframe9a7c2-ripple-enter-duration:80ms;--qxframe9a7c2-ripple-leave-duration:80ms';document.body.appendChild(autoButton);await sleep(80);autoButton.dispatchEvent(new PointerEvent('pointerdown',{bubbles:true,clientX:10,clientY:10,pointerId:3,pointerType:'mouse'}));await sleep(0);a(autoButton.querySelectorAll('.qxframe9a7c2-ripple-wave.is-inside').length===1,'Button .is-ripple must auto-enhance through Ripple');autoButton.dispatchEvent(new PointerEvent('pointerup',{bubbles:true,clientX:10,clientY:10,pointerId:3,pointerType:'mouse'}));autoButton.remove();
const dp=DatePicker.create({document,container:host(),defaultValue:'2026-08-01',needConfirm:true,presets:[{label:'Ten',value:'2026-08-10'},{label:'Eleven',value:'2026-08-11'}]});dp.open('verify-preset');await sleep(30);const pr=dp.getPresetsElement(),buttons=[...pr.querySelectorAll('.qxframe9a7c2-date-picker-preset')];pr.focus();key(pr,'ArrowDown');await sleep(0);const vf=pr.querySelectorAll('.is-keyboard-focus');a(document.activeElement===pr&&vf.length===1&&vf[0]===buttons[1],'DatePicker preset real focus must remain on owner while virtual focus moves');const hostStyle=getComputedStyle(pr),itemStyle=getComputedStyle(vf[0]);a(hostStyle.outlineStyle==='none'||hostStyle.outlineWidth==='0px','DatePicker preset owner must not show focus outline');a(itemStyle.outlineStyle!=='none'&&itemStyle.outlineWidth!=='0px','DatePicker active preset must show keyboard outline');dp.destroy();
out.textContent='FINALREG:'+JSON.stringify({ok:true,modules:${files.size}});
}catch(e){out.textContent='FINALREG:'+JSON.stringify({ok:false,error:String(e&&e.stack||e)})}`;
const html=`<!doctype html><meta charset=utf-8><style>${safeCss}</style><div id=result>pending</div><script type=importmap>${importMap}</script><script type=module>${test}</script>`;
const profile='/tmp/qx-final-reg-'+process.pid+'-'+Date.now();
const child=cp.spawn(browser,['--headless=new','--no-sandbox','--disable-gpu','--disable-dev-shm-usage','--disable-background-networking','--no-first-run','--remote-debugging-port=0','--user-data-dir='+profile,'about:blank'],{stdio:['ignore','ignore','pipe']});
let stderr='',endpoint=null;child.stderr.setEncoding('utf8');child.stderr.on('data',c=>{stderr+=c;const m=stderr.match(/DevTools listening on (ws:\/\/[^\s]+)/);if(m)endpoint=m[1];});
const deadline=Date.now()+30000;while(!endpoint&&Date.now()<deadline)await new Promise(r=>setTimeout(r,50));if(!endpoint)throw new Error('CDP endpoint timeout: '+stderr.slice(-1000));
const socket=new WebSocketClient(endpoint);await new Promise((r,j)=>{socket.onopen=r;socket.onerror=()=>j(new Error('CDP connect failed'));});
let id=0;const pending=new Map();socket.onmessage=e=>{const m=JSON.parse(e.data);if(!m.id||!pending.has(m.id))return;const p=pending.get(m.id);pending.delete(m.id);m.error?p.j(new Error(m.error.message)):p.r(m.result);};
function call(method,params={},sessionId){return new Promise((r,j)=>{const n=++id;pending.set(n,{r,j});const msg={id:n,method,params};if(sessionId)msg.sessionId=sessionId;socket.send(JSON.stringify(msg));});}
let targetId=null;
try{({targetId}=await call('Target.createTarget',{url:'about:blank'}));const {sessionId}=await call('Target.attachToTarget',{targetId,flatten:true});await call('Page.enable',{},sessionId);await call('Runtime.enable',{},sessionId);const tree=await call('Page.getFrameTree',{},sessionId);await call('Page.setDocumentContent',{frameId:tree.frameTree.frame.id,html},sessionId);let payload=null;const until=Date.now()+25000;while(Date.now()<until){const r=await call('Runtime.evaluate',{expression:`(document.getElementById('result')||{}).textContent||''`,returnByValue:true},sessionId);const text=r?.result?.value;if(typeof text==='string'&&text.startsWith('FINALREG:')){payload=JSON.parse(text.slice(9));break;}await new Promise(r=>setTimeout(r,100));}if(!payload)throw new Error('result timeout');if(!payload.ok)throw new Error(payload.error);console.log(JSON.stringify({...payload,structural:true}));}
finally{if(targetId)await call('Target.closeTarget',{targetId}).catch(()=>{});try{socket.close()}catch{};try{child.kill('SIGKILL')}catch{};try{fs.rmSync(profile,{recursive:true,force:true})}catch{}}
