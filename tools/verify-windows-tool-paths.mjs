import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');
const files=['tools/verify-final-audit-regressions.mjs','tools/verify-phase-c-composite.mjs','tools/verify-phase-c-popup-composites.mjs','tools/verify-phase-c-select.mjs','tools/verify-phase-c-tail.mjs','tools/verify-phase-d-popup-selection.mjs','tools/verify-phase-d-table-selection.mjs','tools/verify-phase-d-tags-selection.mjs','tools/verify-phase-e-foundation.mjs'];for(const rel of files){const s=fs.readFileSync(path.join(root,rel),'utf8');assert.doesNotMatch(s,/new URL\(import\.meta\.url\)\.pathname/,rel+' must not use URL pathname as a Windows filesystem path');assert.match(s,/fileURLToPath\(import\.meta\.url\)/,rel+' must use fileURLToPath(import.meta.url)');}console.log(JSON.stringify({ok:true,files:files.length,platform:process.platform}));
